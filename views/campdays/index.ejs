<h1>Camp Days</h1>
<a href="/campdays/new">+ New Camp Day</a>
<ul>
  <% days.forEach(day => { %>
  <li>
    <a href="/campdays/<%= day._id %>"><%= day.title %></a>
    <form action="/campdays/<%= day._id %>/delete" method="POST">
      <button>Delete</button>
    </form>
  </li>
  <% }) %>
</ul>
